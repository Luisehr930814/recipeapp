# This project uses only the Python standard library for the command-line version. For the web interface and OCR features, install the extra dependencies listed below.

# Flask is required for the optional web interface.
flask

# These packages are required for OCR-based ingredient scanning using Tesseract.
pytesseract
pillow

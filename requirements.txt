# This project uses only the Python standard library, so no external
# dependencies are required. If you extend the project with OCR
# libraries (e.g. pytesseract), add them here.

# For the optional Flask web interface, install Flask.
flask